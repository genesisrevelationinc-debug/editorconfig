# File Exclusion Properties

EditorConfig now supports properties for excluding files and folders from listings and search indexes.

## Properties

### `file_exclude_patterns`

A comma-separated list of file name patterns that should be excluded from file listings.

Example:
